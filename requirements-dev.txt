# Development & CI dependencies (runtime deps come from requirements.txt).
-r requirements.txt
ruff==0.16.0  # pinned: ruff's formatter output changes between releases (keep CI == local)
requests>=2.31
